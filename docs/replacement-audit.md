# Agentic Harness Replacement Audit

This audit tracks Agentic Harness against the previous TypeScript feature
surface. It is intentionally evidence-based: each row points at current Rust
code, tests, or docs that cover the requirement.

## Summary

Agentic Harness is a native Rust SDK and CLI for local, self-hosted, CI, and
remote-sandbox agent workflows. The primary product direction is coding agents
running against a local checkout or a remote Linux sandbox such as Vercel
Sandbox, Daytona, or E2B. Cloudflare is intentionally optional edge
control/webhook infrastructure, not the main coding-agent runtime. The target
split is documented in [`execution-targets.md`](execution-targets.md).

The current supported deployment path is a native Rust binary. `--target node`
is accepted as a compatibility alias for old examples, but still builds and
runs the native server artifact. `--target cloudflare` is build-only: it writes
non-proxy Worker boundary artifacts, but `run` and `dev` fail explicitly instead
of pretending a native binary can deploy to Workers.

## Gap Checklist

| # | Original gap | Current status | Evidence |
|---|---|---|---|
| 1 | Model/provider runtime | Covered for native Rust runtime and now split at a non-native HTTP transport boundary. Supports provider/model strings, provider settings, env API key resolution, role/call/default precedence, OpenAI-compatible HTTP models, explicit JSON runtime config loading, workspace runtime config discovery, and custom `HttpModelTransport` implementations for Worker/WASM adapters. | `ProviderSettings`, `ProvidersConfig`, `ModelConfig`, `AgentRuntimeConfig`, `OpenAiCompatibleModel`, `HttpModelTransport` in `crates/agentic-harness/src/lib.rs`; docs `docs/runtime-config.md`; tests `configured_models_resolve_default_role_and_call_precedence`, `provider_settings_*`, `openai_compatible_model_builds_chat_request_and_reads_response`, `no_default_openai_compatible_model_uses_custom_http_transport`, `runtime_config_*`, `load_workspace_context_discovers_*runtime_config_file`. |
| 2 | Tool calling | Covered. Supports `ToolDef`, JSON schema parameters, app/per-call tools, duplicate and builtin-name rejection, direct invocation, model-requested tool loops, OpenAI-compatible `tool_calls`, and built-in tools. | `ToolDef`, `ToolSpec`, `ToolCall`, `execute_tool_call`, `builtin_tool_specs`; tests `custom_tools_are_registered_invokable_and_passed_to_model_requests`, `custom_tools_reject_duplicate_and_builtin_names`, `session_executes_model_requested_tool_calls_before_returning_final_response`, `session_exposes_builtin_tools_to_models_and_executes_builtin_tool_calls`. |
| 3 | Sandbox/command isolation | Covered for the runtime layer. Agentic Harness has a `SessionEnv` contract, host command registration, cwd/env/timeout shell options, custom remote env binding, `MemorySessionEnv` for empty in-memory filesystems, and `HttpSessionEnv` for JSON-over-HTTP remote sandboxes. `HttpSessionEnv::with_transport` is no-default compatible so Worker/WASM adapters can provide platform HTTP. Provider-owned hosted sandboxes remain connector modules in the user's Rust project, matching the current connector-based design. | `SessionEnv`, `MemorySessionEnv`, `HttpSessionEnv`, `HttpSessionTransport`, `CommandDef`, `ShellOptions`; protocol doc `docs/http-session-env.md`; tests `shell_options_scope_cwd_env_timeout_and_file_helpers`, `registered_commands_are_explicit_host_capabilities`, `sessions_can_run_file_and_shell_helpers_against_custom_session_env`, `memory_session_env_provides_empty_isolated_file_helpers`, `http_session_env_*`, `no_default_http_session_env_uses_custom_transport_for_remote_sandboxes`. |
| 4 | `task()` / child delegation | Covered. `Session::task`, `task_with_id`, and model-visible built-in `task` run detached child histories with shared context and environment. | `Session::task`, `Session::task_with_id`, builtin `task` handling; tests `session_task_runs_in_detached_child_history`, `session_exposes_builtin_tools_to_models_and_executes_builtin_tool_calls`. |
| 5 | Structured results | Covered for native Rust. Supports result block extraction and schema-guided prompt instructions with typed `DeserializeOwned` output. | `PromptResponse::result_json`, `PromptOptions::result_schema`, `Session::prompt_json_with_options`; tests `prompt_response_extracts_last_structured_json_result`, `prompt_options_can_add_schema_guidance_and_return_typed_json`. |
| 6 | Compaction/session history parity | Covered for the native runtime and now split at a runtime-agnostic storage boundary. Supports persisted `SessionData`, message/compaction/branch entries, custom `SessionStore` implementations, fallible session construction for storage load errors, native file-backed session storage, manual compaction, branch summaries, and token-aware auto-compaction. | `SessionData`, `SessionEntry`, `SessionStore`, `FileSessionStore`, `CompactionSettings`; tests `no_default_core_accepts_custom_session_store`, `no_default_try_session_with_env_surfaces_store_load_errors`, `file_session_store_persists_history_across_app_instances`, `file_session_store_persists_compaction_and_branch_summary_entries`, `prompt_options_auto_compact_over_budget_context_before_model_call`. |
| 7 | MCP | Covered for streamable HTTP and legacy SSE MCP tool adaptation. | `McpServerOptions`, `McpTransport`, `connect_mcp_server`, `mcp_tools_from_client`; tests `mcp_client_tools_are_exposed_as_custom_tools`, `mcp_server_options_expose_legacy_sse_transport_selection`, internal legacy SSE parser tests. |
| 8 | HTTP streaming/event parity | Covered for synchronous JSON, SSE event endpoints in the native server, a platform-neutral `HttpRequest` entrypoint that lets Worker-style adapters select SSE with `Accept: text/event-stream` on `/agents/<name>/<id>`, live event sinks that emit during handler execution, and reusable SSE frame encoding. | `AgentApp::handle_http`, `AgentApp::handle_request`, `AgentApp::invoke_with_event_sink`, `HttpRequest`, `RuntimeEvent`, `RuntimeEvent::to_sse_frame`, `/agents/<name>/<id>/events`, `HttpResponse::sse`, runtime event emission; tests `http_dispatch_can_return_sse_event_stream`, `http_request_accept_header_can_select_sse_on_agent_route`, `http_event_stream_includes_model_tool_events`, `runtime_events_can_stream_to_a_live_sink`, `runtime_event_encodes_sse_frames`, `no_default_core_dispatches_sse_from_accept_header`, `no_default_core_can_stream_runtime_events_to_a_sink`, `no_default_runtime_event_encodes_sse_frames`. |
| 9 | Cloudflare/Node build targets | Covered for the intended current scope. `native` is the real target, `node` is a compatibility alias, and `cloudflare` generates non-proxy Worker boundary files for edge/control output while `run` remains rejected. Core derives Cloudflare Worker metadata for Durable Object bindings/classes, validates route-safe agent names, emits the Wrangler JSON fragment for bindings and SQLite migrations, emits a non-proxy Worker routing module, and the CLI writes `_entry.js`, `wrangler.jsonc`, `cloudflare-manifest.json`, `agentic_harness_worker.js`, `agentic_harness_app.js`, and `agentic_harness_app.d.ts`. The Worker runtime shim handles JSON parsing, SSE, webhook acceptance, and Durable Object SQL session plumbing. Core exposes `WorkerAppRequest`, `WorkerAppResponse`, `AgentApp::handle_worker_app_request`, and `agentic_harness_worker_app!` for the generated WASM JSON ABI. `--worker-app <path>` can link a Worker-compatible app adapter, `--worker-wasm <path>` can package its companion WASM module, and `--worker-wasm-crate <path>` can build/package a Rust adapter crate for `wasm32-unknown-unknown`; with WASM alone the CLI emits default JSON ABI glue. Full Workers deployment compatibility is deliberately not claimed because Cloudflare is not the primary coding-agent execution target. | `BuildTarget::parse`, `build_cloudflare`, `build_worker_wasm_crate`, `cloudflare_worker_runtime_module`, `cloudflare_app_adapter_stub`, `cloudflare_default_wasm_app_adapter`, `cloudflare_app_adapter_types` in `crates/agentic-harness-cli/src/lib.rs`; `AgentApp::cloudflare_worker_manifest`, `CloudflareWorkerManifest`, `CloudflareDurableObjectBinding`, `CloudflareWorkerManifest::wrangler_config`, `CloudflareWorkerManifest::worker_entrypoint`, `WorkerAppRequest`, `WorkerAppResponse`, `AgentApp::handle_worker_app_request`, `agentic_harness_worker_app!`; tests `node_target_is_a_native_compatibility_alias_for_run_and_build`, `cloudflare_run_reports_native_runtime_incompatibility`, `cloudflare_build_generates_non_proxy_worker_artifacts`, `cloudflare_build_can_link_a_worker_app_adapter`, `cloudflare_build_can_package_a_worker_wasm_module`, `cloudflare_build_generates_default_wasm_adapter_when_only_wasm_is_provided`, `cloudflare_build_can_compile_and_package_a_worker_wasm_crate`, `manifest_can_print_cloudflare_worker_metadata_from_a_native_workspace`, `cloudflare_worker_manifest_maps_webhook_agents_to_durable_object_bindings`, `cloudflare_worker_manifest_rejects_agent_names_that_cannot_route_to_bindings`, `cloudflare_worker_manifest_generates_wrangler_config_fragment`, `cloudflare_worker_manifest_generates_non_proxy_worker_entrypoint`, `no_default_core_handles_worker_app_json_abi_requests`, `no_default_worker_app_macro_exports_wasm_json_abi`, `no_default_core_exposes_cloudflare_worker_bindings_for_webhook_agents`, `no_default_cloudflare_manifest_generates_wrangler_config_fragment`, `no_default_cloudflare_manifest_generates_non_proxy_worker_entrypoint`. |
| 10 | Local HTTP hosting and `dev` watch/reload | Covered for native local hosting. `agentic-harness setup hosting` stores loopback-only host config, `agentic-harness hosting status --json` exposes local URLs and HTTP capabilities, `agentic-harness host` starts the configured native server, and `agentic-harness host --dev` / `agentic-harness dev` watch workspace changes and restart the child server while ignoring generated/VCS directories. This is local hosting only, not deployment. | `Commands::Host`, `Commands::Hosting`, `SetupCommands::Hosting`, `HostingConfig`, `HostingStatus`, `setup_hosting_command`, `hosting_status`, `format_hosting_status_json`, `run_dev_server`, `watch_snapshot`, `wait_for_watch_change`; tests `local_hosting_setup_status_and_dashboard_json_expose_urls`, `watch_snapshot_detects_file_add_modify_and_delete`, `watch_snapshot_ignores_generated_and_vcs_directories`. |
| 11 | Connector registry parity | Covered for native connector instructions and category roots. The CLI lists connector category/provider metadata, resolves aliases case-insensitively, emits raw instructions when piped to Claude/Codex-style agents, includes named Daytona, Vercel Sandbox, and E2B sandbox recipes, and supports generic category instructions. | `native_connectors`, `native_category_roots`, `native_daytona_connector_markdown`, `native_vercel_connector_markdown`, `native_e2b_connector_markdown`, `resolve_native_connector`, `should_print_agent_instructions`; tests `add_lists_and_prints_native_connector_instructions`, `add_category_roots_emit_agent_ready_connector_instructions`. |
| 12 | File/search helpers | Covered. Helpers include bounded reads with truncation metadata, write/edit/stat/readdir/exists/mkdir/rm, regex grep, wildcard glob with `*` and `?`, and shell cwd/env/timeout options through `SessionEnv`. | `ReadOptions`, `ReadOutput`, file helper methods, `grep_env`, `glob_env`; tests `context_exposes_native_file_search_and_edit_tools`, `read_with_options_returns_bounded_content_and_truncation_metadata`, `grep_supports_regex_and_glob_supports_question_mark`, `shell_options_scope_cwd_env_timeout_and_file_helpers`. |
| 13 | Starter agent creation options | Covered for native Rust starter projects. `agentic-harness new --template` can create `hello`, CLI-only `triage`, workspace `data`, repository `coding`, `code-review`, `test-fixer`, `repo-analyst`, and customer `support` agents, each with a Rust handler, `AGENTS.md`, a role, and a starter skill for coding-agent follow-up. | `Commands::New`, `ScaffoldTemplate`, `BUILT_IN_TEMPLATES`, `scaffold_*_main_rs` in `crates/agentic-harness-cli/src/lib.rs`; tests `new_scaffolds_a_native_rust_agent_project`, `new_scaffolds_source_style_agent_templates`; smoke runs generated `coding` and `triage` projects. |
| 14 | Setup and onboarding checks | Covered. `agentic-harness` opens the TUI in a TTY and prints the plain menu map in non-TTY shells. `agentic-harness wizard`, `agentic-harness tui`, and `agentic-harness ui` open the same workspace status panel for readiness, sandbox target, local hosting status, template counts, recent logs, and next step, then show a two-step numbered TUI-style flow focused on coding, templates, LLM authoring setup, sandbox setup, local running/hosting, and doctor workflows, with `b`/`q` escape routes and no deployment path in the menu. Second-level selection screens include contextual panels for coding LLM availability, latest results, template registry JSON, preview JSON, LLM authoring setup, sandbox status JSON, sandbox log JSON, local hosting commands, example manifests, readiness, and next fixes. The guided LLM coding shortcut uses `agentic-harness code --llm auto` so it follows Claude Code, Codex, Cursor, and Wind Server detection instead of forcing Codex. Template install/import actions ask for workspace/user/team registry scope instead of forcing a long command. `agentic-harness guide`/`quickstart`/`onboard` prints the ordered install, LLM setup, template-authoring, coding, and inspection path, with `--json` for software agents. `agentic-harness dashboard`/`status` shows workspace readiness, local hosting, template registry metadata, template-authoring briefs, sandbox target/smoke status, recent sandbox logs, and next commands, with `--json` exposing the same status as machine-readable data for software agents. `agentic-harness doctor`/`check` validates a workspace before running by checking the Rust manifest, entrypoint, instructions, roles, skills, sandbox readiness, and LLM template-authoring setup, then prints actionable fixes; `--json` exposes checks, fixes, readiness, optional warnings, and next command without scraping. | `Commands::Wizard`, `Commands::Guide`, `Commands::Dashboard`, `Commands::Doctor`, `default_wizard_command`, `guide_command`, `guide_steps`, `format_guide_report`, `format_guide_json`, `render_wizard_status_panel`, `render_wizard_step_panel`, `render_coding_wizard_panel`, `render_template_wizard_panel`, `render_llm_wizard_panel`, `render_sandbox_wizard_panel`, `render_run_wizard_panel`, `render_check_wizard_panel`, `llm_tool_status`, `CODE_CHOICES`, `TEMPLATE_CHOICES`, `LLM_CHOICES`, `SANDBOX_CHOICES`, `RUN_CHOICES`, `CHECK_CHOICES`, `prompt_template_scope_default`, `format_dashboard_report`, `format_dashboard_json`, `dashboard_hosting_json`, `dashboard_template_json_entries`, `dashboard_template_brief_entries`, `format_doctor_report`, `format_doctor_json` in `crates/agentic-harness-cli/src/lib.rs`; tests `bare_command_prints_plain_tui_map_in_non_tty`, `tui_and_ui_aliases_open_the_wizard_dashboard`, `wizard_plain_renders_runtime_dashboard_and_next_commands`, `plain_wizard_dashboard_has_no_ansi_escape_codes`, `styled_wizard_dashboard_uses_clack_style_color_and_markers`, `wizard_selection_screens_include_contextual_status_panels`, `dashboard_plain_shows_workspace_templates_sandbox_logs_and_next_steps`, `dashboard_json_exposes_agent_readable_status`, `local_hosting_setup_status_and_dashboard_json_expose_urls`, `guide_prints_start_here_path_for_humans_and_agents`, `short_status_and_check_aliases_cover_dashboard_and_doctor`, `wizard_workflows_expose_multiple_selectable_options`, `wizard_template_registry_actions_show_scope_selection`, `doctor_reports_ready_generated_project`, `doctor_reports_missing_workspace_with_next_step`, `doctor_json_reports_required_and_optional_checks`. |
| 15 | Template, LLM setup, sandbox setup, and one-command coding flow | Covered for the local CLI slice. `agentic-harness template` can initialize a valid reusable pack with version metadata, create an LLM handoff brief after authoring setup, include a current local CLI fallback path in that brief when `agentic-harness` is not on PATH, run the selected Claude Code, Codex, Cursor, or Wind Server command from the target workspace with `template author --open`, stream live LLM output while retaining captured output for diagnostics, list built-ins and workspace/user/team scoped packs, show verbose or JSON registry metadata, preview built-in/scoped/path templates as text or JSON, validate local template packs, install/import packs into `.agentic-harness/templates`, `.agentic-harness/templates/user`, or `.agentic-harness/templates/team`, export packs to shareable folders, and `agentic-harness new --template <name-or-path>` can render installed or local packs. Short aliases such as `tpl create`, `templates ls`, `tpl preview`, `templates check`, `tpl add`, and `tpl use` cover the common human path. `agentic-harness setup llm` installs template-authoring instructions for Claude Code, Codex, Cursor, and Wind Server plus a valid `.agentic-harness/template-examples/code-review` reference pack that the installed skill points to and `template validate` accepts. `agentic-harness setup sandbox` configures local checkout execution, prints named Vercel Sandbox, Daytona, E2B, or custom remote `SessionEnv` connector instructions, or stores an HTTP SessionEnv endpoint for remote operations. `agentic-harness sandbox` can show local/remote status as text or JSON, probe configured HTTP endpoints with remote `pwd` and directory-list smoke checks, run shell commands as text or JSON, read files, write files, list directories, sync host files into the local or HTTP remote sandbox, show operation logs as text or ordered JSON entries, and clean up sandbox paths. `agentic-harness start`/`code` checks setup, inspects the repository, builds deterministic `plannedSteps`, passes inspection/plan/check context, workspace instruction files, project manifest files, git diff stat, and changed-file context into the `code` agent, emits stderr progress for inspect/plan/agent/patch/repair/check/commit/PR/summary stages without corrupting stdout, can apply prepared unified diff patches or an agent-returned `generatedPatch`, can launch Claude Code, Codex, Cursor, or Wind Server through `code --llm` with a generated coding brief before checks, uses `codex exec` with brief stdin for Codex, `claude -p --permission-mode acceptEdits` for Claude Code, and `cursor agent --print --trust --force` for Cursor, streams LLM output to stderr during coding runs while preserving captured output in summaries, can resolve `--llm auto` from installed commands before falling back to Codex, runs explicit or auto-detected checks locally or through the configured remote `SessionEnv` sandbox, makes one repair call with `failedChecks` when checks fail, can create an explicit git commit after successful checks, can open a pull request with `gh pr create --fill`, writes default latest Markdown and JSON coding-run summaries when no explicit summary path is provided, `agentic-harness result`/`inspect`/`last` can read the latest summary, and custom summaries still include workspace-instruction, project-context, plan, LLM-tool, and changed-files sections. | `Commands::Code`, `Commands::Result`, `Commands::Template`, `Commands::Setup`, `Commands::Sandbox`, `TemplateCommands`, `TemplatePack`, `TemplateScope`, `init_template_pack`, `template_author_command`, `open_llm_authoring_environment`, `llm_authoring_invocation`, `render_template_authoring_brief`, `current_cli_path`, `format_template_list_json`, `format_template_preview_json`, `format_sandbox_status_json`, `format_sandbox_logs_json`, `format_sandbox_exec_json`, `run_sandbox_smoke`, `export_template_pack`, `install_template_pack`, `install_template_authoring_examples`, `setup_llm_command`, `setup_sandbox_command`, `sandbox_command`, `remote_sandbox_env`, `native_e2b_connector_markdown`, `code_command`, `run_llm_coding_tool`, `write_coding_llm_brief`, `command_available`, `llm_prompt_invocation`, `llm_coding_invocation`, `run_llm_invocation_with_echo`, `capture_child_stream`, `run_coding_check`, `result_command`, `coding_progress`, `coding_plan_steps`, `detect_project_files`, `git_diff_stat`, `git_changed_files`, `write_agent_generated_patches`, `apply_coding_patch`, `commit_coding_run`, `create_coding_pull_request`, `coding_checks_for_workspace`, `load_workspace_instructions`, `workspace_instruction_json_entries`, `changed_files_from_coding_run`, `write_coding_summary`, `write_coding_summary_json`, `render_coding_summary_json`, `print_template_preview`, `scaffold_any_template` in `crates/agentic-harness-cli/src/lib.rs`; tests `template_aliases_support_short_human_commands`, `template_init_creates_valid_pack_that_can_install_and_scaffold`, `template_author_creates_llm_handoff_brief`, `template_author_open_launches_the_selected_llm_command`, `template_author_open_streams_llm_output_before_process_exits`, `cursor_llm_invocation_uses_headless_print_mode`, `template_registry_lists_template_versions`, `template_registry_lists_machine_readable_json`, `template_preview_can_print_machine_readable_json`, `template_registry_can_export_and_import_template_packs`, `template_registry_supports_user_and_team_scopes`, `template_pack_can_validate_install_and_scaffold_project`, `setup_llm_installs_template_authoring_context_and_doctor_detects_it`, `setup_sandbox_local_updates_doctor_and_remote_prints_instructions`, `sandbox_status_json_exposes_agent_readable_capabilities`, `sandbox_status_json_smokes_remote_endpoint`, `sandbox_local_exec_read_write_and_list_work_from_cli`, `sandbox_local_sync_logs_and_cleanup_work_from_cli`, `sandbox_custom_endpoint_runs_remote_operations_from_cli`, `code_command_runs_the_coding_template_with_one_short_command`, `start_alias_runs_the_coding_flow`, `code_command_writes_latest_summary_and_inspect_reads_it`, `code_command_writes_run_summary_and_executes_checks`, `code_command_runs_checks_in_configured_remote_sandbox`, `code_command_applies_patch_before_checks_and_summary`, `code_command_applies_agent_generated_patch_before_checks`, `code_command_can_launch_llm_coder_before_checks`, `code_command_auto_llm_uses_first_installed_coder_before_checks`, `code_command_retries_failed_checks_with_repair_patch`, `code_command_can_commit_successful_coding_run`, `code_command_can_create_pull_request_after_successful_run`. |
| 16 | Release packaging | Covered for the pre-release distribution slice. The CLI exposes `--version`, `agentic-harness smoke --json`, `agentic-harness package --output dist/packages --json`, and `agentic-harness release-check --json`; the repo includes a local install script, a Homebrew HEAD formula, a changelog, and a clean-machine release smoke-test checklist. The package command stages the current CLI binary into a versioned OS/architecture folder with `manifest.json` and `SHA256SUMS`. The release check validates the install script, formula, binary package docs, changelog, and smoke checklist before publishing. Stable release publishing still needs a real GitHub tag, release artifacts, and upstream tarball SHA when the repository is moved to its final upstream. | `scripts/install.sh`, `Formula/agentic-harness.rb`, `CHANGELOG.md`, `docs/release-smoke-test.md`, `Commands::Smoke`, `Commands::Package`, `Commands::ReleaseCheck`, `smoke_command`, `package_command`, `package_binary`, `sha256_hex`, `release_check_command`, `release_packaging_checks`, `release_binary_package_check`, `Cli` version metadata in `crates/agentic-harness-cli/src/lib.rs`; tests `release_packaging_artifacts_are_locally_smoke_testable`, `package_command_creates_binary_distribution_manifest_and_checksums`, `release_check_reports_packaging_readiness_as_text_and_json`, `smoke_command_reports_install_readiness_as_text_and_json`. |

## Deliberate Non-Goal

Full Cloudflare Workers handler execution is not part of the current replacement
scope. The Rust CLI generates a Worker and Durable Object entrypoint, Wrangler
config, a Worker runtime shim, Durable Object SQL session plumbing, a Rust-side
WASM ABI macro, an adapter-crate compile/package path, and transport boundaries
for Worker-safe model and sandbox HTTP. That is enough for edge/control
artifacts and for inspecting platform boundaries.

Agentic Harness should not advertise full Workers deployment compatibility
unless a future release intentionally wires a Worker-compatible WASM or
JavaScript entrypoint with sandbox and persistence adapters end to end. The
concrete runtime boundary is tracked in
[`docs/cloudflare-runtime.md`](cloudflare-runtime.md).

The more important product work is the operator surface: a simpler TUI owned by
Agentic Stack, plus first-class local and remote sandbox execution for coding
agents. Agentic Harness should stay the native Rust runtime behind that surface.

## Verification Gates

The current Rust workspace should stay green under:

```bash
cargo fmt --all --check
cargo test --workspace
cargo clippy --workspace -- -D warnings
```

## Live Dogfood Evidence

- 2026-05-07: `agentic-harness template author reviewer --env codex --open`
  was run in a disposable workspace with the installed Codex authoring context.
  Codex created a reusable `reviewer` template pack, installed it, scaffolded
  `reviewer-agent`, and reported successful acceptance checks.
- Independent verification after the live run passed:
  `agentic-harness template validate /tmp/agentic-harness-template-dogfood.MOXHiS/reviewer`,
  `agentic-harness doctor --workspace /tmp/agentic-harness-template-dogfood.MOXHiS/reviewer-agent`,
  and
  `cargo check --offline --manifest-path /tmp/agentic-harness-template-dogfood.MOXHiS/reviewer-agent/Cargo.toml`.
- 2026-05-08: live Claude Code dogfood ran
  `agentic-harness template author reviewer --env claude-code --open` in a
  disposable workspace with the installed Claude Code authoring context. Claude
  Code created a reusable `reviewer` template pack using the required manifest,
  role, skill, Rust entrypoint, and sample payload shape. Claude Code could not
  run the acceptance commands itself because `agentic-harness` was not on
  `PATH` inside the disposable workspace.
- Independent verification after the Claude Code run passed:
  `agentic-harness template validate /tmp/agentic-harness-claude-dogfood.Y8ar0E/reviewer`,
  `agentic-harness template install /tmp/agentic-harness-claude-dogfood.Y8ar0E/reviewer --workspace /tmp/agentic-harness-claude-dogfood.Y8ar0E`,
  `agentic-harness new ./reviewer-agent --template reviewer` from the dogfood
  workspace,
  `agentic-harness doctor --workspace /tmp/agentic-harness-claude-dogfood.Y8ar0E/reviewer-agent`,
  `cargo check --manifest-path /tmp/agentic-harness-claude-dogfood.Y8ar0E/reviewer-agent/Cargo.toml`,
  and
  `agentic-harness run reviewer --workspace /tmp/agentic-harness-claude-dogfood.Y8ar0E/reviewer-agent --id demo --payload '{"prompt":"Inspect this repository"}'`.
